# Note: Labels are automatically created by the setup-labels.yml workflow.
# The workflow runs on push to main and can also be triggered manually.
# If labels don't exist yet, run the workflow manually from the Actions tab.

version: 2
updates:
  # npm package updates
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    commit-message:
      prefix: "chore(deps):"
      include: "scope"
    assignees:
      - "cannaplan"
    labels:
      - "dependencies"
      - "automated"
    open-pull-requests-limit: 10
    versioning-strategy: auto

  # GitHub Actions updates
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    commit-message:
      prefix: "chore(deps):"
      include: "scope"
    assignees:
      - "cannaplan"
    labels:
      - "dependencies"
      - "github-actions"
    open-pull-requests-limit: 5
